const styles = (theme) => ({
  styleWelcomeContainer: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
    userSelect: "none",
    marginTop: "1rem",
    marginBottom: "1rem",
    position: "relative",
  },
});

export default styles;
