const styles = (theme) => ({
  styleHeader: {
    display: "flex",
    flexFlow: "row",
    justifyContent: "space-between",
  },
  styleHeaderTitle: {
    fontWeight: "bold",
    userSelect: "none",
  },
});

export default styles;
